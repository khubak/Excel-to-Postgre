# Excel-to-Postgre
A python script for data extraction from Excel sheets that loads extracted data into PostgreSQL DB

A python script for data extraction from Excel sheets that loads extracted data into PostgreSQL DB
